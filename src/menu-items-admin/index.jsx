// project-imports
import buySeans from './calender';
import sessionHistory from './session-history';
import activeUsers from './active-users';
import otp from './otp';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [buySeans, sessionHistory, activeUsers, otp]
};

export default menuItems;
