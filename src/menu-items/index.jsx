// project-imports
import buySeans from './buy-session';
import sessionHistory from './session-history';
import accountInfo from './account-info';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [buySeans, sessionHistory, accountInfo]
};

export default menuItems;
